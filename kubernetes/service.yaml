apiVersion: v1
kind: Service
metadata:
  name: kafka-timescale-ingestor
  namespace: trading
spec:
  selector:
    app: kafka-timescale-ingestor
  ports:
  - name: health
    port: 8001
    targetPort: health
  - name: prometheus
    port: 8002
    targetPort: prometheus
  type: ClusterIP 